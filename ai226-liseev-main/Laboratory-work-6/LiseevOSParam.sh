#!/bin/bash

read par1 info

if [[ $par1 =~ "Cached" ]]
then
	grep '^Cached' /proc/meminfo
	if [[ $info = "info" ]]
	then
	echo 'обсяг зайнято під кеш читання сторінок з диску (файли, директорії, файли
блокових пристроїв, дані, які стосуються механізму IPC, дані процесів рівня користувача,
скинутих в область підкачки), але не включає в себе SwapCached'
	fi
else
        echo "Параметр не знайдено"
fi 
